name: Ruff Lint

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    uses: ./.github/workflows/python_check.yml
    with:
      job_name: Ruff lint
      command: uv run ruff check .
      python_versions: '["3.13"]'
